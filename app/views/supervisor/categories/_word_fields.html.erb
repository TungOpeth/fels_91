<div class="fields">
  <p>
    <%= f.hidden_field :id %>
    <%= f.label :content, (t :Word)%>
    <%= f.text_field :content%>
    <%= link_to_remove_fields (t :remove), f %>
  </p>
    <%= f.fields_for :word_answers do |builder|%>
    <%= render 'word_answer_fields', f: builder %>
  <% end %>
  <p>
    <%= link_to_add_fields (t :AddNewWordAnswer), f, :word_answers %>
  </p>
</div>

