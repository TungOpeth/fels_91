<% if @lesson.result == nil %>
<h1>new lesson</h1>
<li>
  <p>
  <%= t "Category" %>: <%= link_to @lesson.category.name, @lesson.category %>
  <%= t "has" %>: <%= @lesson.words.count %> <%= t "words" %>
  </p>
  <%= form_for @lesson do |f| %>
    <%= f.fields_for :lesson_words do |lesson_word| %>
      <%= render "word_form", f: lesson_word %>
    <% end %>
    <%= f.submit (t :submit) %>
  <% end %>
</li>
<% else %>
  <%= render "result" %>
<% end %>
